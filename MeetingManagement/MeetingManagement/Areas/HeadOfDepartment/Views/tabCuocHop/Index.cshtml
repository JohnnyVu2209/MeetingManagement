@model IEnumerable<MeetingManagement.Models.CATEGORY>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/HeadOfDepartment/Views/Shared/_Layout.cshtml";
}

<link href="~/Content/catalog-list.css" rel="stylesheet" />
<link href="~/Content/quyhung/css/tabcuochop.css" rel="stylesheet" />

<br />
<div class="tab">
    <button class="tablinks" onclick="openTab(event, 'mycate')">Danh sách danh mục</button>
    <button class="tablinks" onclick="openTab(event, 'mymeeting')">Danh sách cuộc họp</button>
    <button class="tablinks" onclick="openTab(event, 'mywork')">Danh sách công việc</button>
</div>


<div id="mycate" class="tabcontent">
    <ul class="catalogList">
        @foreach (var item in Model)
        {
            <li>
                @item.Category_Name
            </li>
        }
    </ul>
</div>

<div id="mymeeting" class="tabcontent">
    <ul class="catalogList">
        @foreach (var item in Model)
        {
            <li>
                @item.MEETINGs
            </li>
        }
    </ul>
</div>


<div id="mywork" class="tabcontent">
    <ul class="catalogList">
       
        
            <li>
                Tối nay code chức năng Tạo danh mục
            </li>
        
    </ul>
</div>



<script>
    function openTab(evt, tabName) {
        var i, tabcontent, tablinks;
        tabcontent = document.getElementsByClassName("tabcontent");
        for (i = 0; i < tabcontent.length; i++) {
            tabcontent[i].style.display = "none";
        }
        tablinks = document.getElementsByClassName("tablinks");
        for (i = 0; i < tablinks.length; i++) {
            tablinks[i].className = tablinks[i].className.replace(" active", "");
        }
        document.getElementById(tabName).style.display = "block";
        evt.currentTarget.className += " active";
    }
</script>


