@model IEnumerable<MeetingManagement.Models.CATEGORY>

@{
    ViewBag.Title = "Index";
}
<div class="body-navbar">
    <div class="nav-content-zone">
        <div class="lable-zone1">
            <h2> Danh Mục</h2>
        </div>
    </div>
</div>
<div class="card-body card-body-fixed">
    <div id="time-table" class="user-ctg-index-table-zone">
        <div class="table user-ctg-index-table">
            @foreach (var item in Model)
            {
                <div onclick="location.href = '@Url.Action("Details","Categories", new { id = item.Category_id})';">
                    @item.Category_Name
                </div>
            }
        </div>
        @*<div class="wrapper">
                <ul class="catalogList">
                    @foreach (var item in Model)
                    {

                        <li>
                            <div onclick="location.href = '@Url.Action("Details","Categories", new { id = item.Category_id})';">@item.Category_Name</div>
                        </li>
                    }

                </ul>
            </div>*@
    </div>
</div>

<style>
    .user-ctg-index-table-zone {
        width: 100%;
    }

    .user-ctg-index-table {
    }

        .user-ctg-index-table div {
            width: 100%;
            height: 50px;
            margin-bottom: 10px;
            /*background-color: #66c2ff;*/
            background-color: #e5e5e5;
            color: black;
            padding: 15px 50px;
            border-radius: 3px;
            cursor: pointer;
        }



            .user-ctg-index-table div:hover {
                background-color: #009FE5;
            }

            .user-ctg-index-table div p {
                font-weight: bold;
                font-size: 16px;
            }

    .user-ctg-index-button {
        float: right;
        width: 60px;
        height: 30px;
        background-color: #009FE5;
        color: white;
        border-radius: 5px;
        margin-top: -30px;
        border: 0px;
    }

    .user-ctg-index-header {
        width: 1300px;
        margin: 20px 0px;
    }

        .user-ctg-index-header h1, h2, h3, h4, h5, h6 {
            margin: 0px;
        }

    .user-ctg-index-manipulation {
        float: right;
        margin: 0px 2.5px;
        color: black;
    }
    .card-body-fixed {
        position: relative;
        top: 50px;
        width: 100%;
        height: 800px;
        z-index: 1;
    }
    .lable-zone1 {
        display: inline-block;
        position: absolute;
        color: black;
        line-height: 40px;
    }

</style>

